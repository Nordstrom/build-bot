build:
  commit:
    image: node:6
    commands:
      - npm run commit
    when:
      event: tag
  bundle:
    image: node:6
    commands:
      - echo "hello drone"
      - npm install -g gulp
      - npm install
      - echo "bye bye"
    when:
      event: push
