module.exports = {
	// 1. mgr.start to start build
	// 2. bot.message to show slack message
	// 3. alexa.message to give vocal message
    start() {},

	// 1. mgr.finish to finish build
	// 2. bot.message to show slack message
    finish() {}
}
